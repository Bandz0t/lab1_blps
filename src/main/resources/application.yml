spring:
  application:
    name: youtube-monetization

  datasource:
    url: jdbc:postgresql://127.0.0.1:55432/youtube_monetization
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB

  jta:
    enabled: true

  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false

storage:
  minio-enabled: true
  bucket: videos
  minio:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin

server:
  port: 8080

logging:
  level:
    org.flywaydb: info

copyright:
  vosk-model-path: models/vosk-model-small-en-us-0.15
  banned-words: good,morning,sure,everybody
  ffmpeg-binary: ffmpeg

security:
  jwt:
    # base64 for 32-byte key: "this-is-very-secret-key-for-jwt!!"
    secret: dGhpcy1pcy12ZXJ5LXNlY3JldC1rZXktZm9yLWp3dCEh
    ttl-minutes: 60

app:
  redis:
    enabled: true
  outbox:
    datasource:
      url: jdbc:postgresql://127.0.0.1:5433/youtube_monetization_outbox
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
      unique-resource-name: youtubeMonetizationOutboxXaDs
      min-pool-size: 2
      max-pool-size: 10
    flyway:
      enabled: true
      locations: classpath:db/outbox
    lock:
      ttl-seconds: 30
  messaging:
    moderation:
      channel: moderation.decision
    outbox:
      poll-delay-ms: 1000
